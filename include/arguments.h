#include <stdio.h>
#include <stdlib.h>

/* Receives argc and argv from main. Stores all source filepaths in outsrc (a
 * pointer to a list of strings) */
int readArgs(int argc, char *argv[], char **outsrc[]);
